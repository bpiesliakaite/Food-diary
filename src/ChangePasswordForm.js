import React from 'react';
import { Text, Container } from 'native-base';
import { Modal } from 'react-native';

export default function ChangePassword() {
    return (
        <Container>
            <Text>This is a form to change the password</Text>
        </Container>
    );
}